import { Metadata } from "next";

export const metadata: Metadata = { title: "About | Company" };

const Company = () => {
  return (
    <section>
      <h1>Company Page</h1>
    </section>
  );
};

export default Company;
