import { Metadata } from "next";

export const metadata: Metadata = { title: "About" };

const About = () => {
  return (
    <section>
      <h1>About Page</h1>
    </section>
  );
};

export default About;
