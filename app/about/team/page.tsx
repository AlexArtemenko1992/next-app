import { Metadata } from "next";

export const metadata: Metadata = { title: "About | Team" };

const Team = () => {
  return (
    <section>
      <h1>Team Page</h1>
    </section>
  );
};

export default Team;
